# Core dependencies for movie-bot
requests>=2.28.0
notion-client>=2.0.0
python-dotenv>=0.19.0

# Optional: for better HTTP handling
urllib3>=1.26.0

# Optional: for JSON handling (included with requests but good to specify)
certifi>=2022.12.7 